name: Deploy to Firebase Hosting on Merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Dependencies
        # Añadimos --legacy-peer-deps para solucionar el error de React 19
        run: npm install --legacy-peer-deps

      - name: Build Project
        run: npm run build:clean

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_CUPCOIN }}'
          channelId: live
          # ¡OJO! Cambia 'tu-proyecto-id' por el ID real de tu proyecto en Firebase
          projectId: cupcoin-b2b4f
          
